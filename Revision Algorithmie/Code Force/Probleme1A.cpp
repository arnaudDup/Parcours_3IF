/*#include <iostream>
using namespace std;

int main ()
{
    unsigned long long  largeur;
    unsigned long long longueur;
    unsigned long long rectangle;
    unsigned long long reponse = 0;
    cin >> largeur >> longueur >> rectangle;
    if ( largeur % rectangle == 0 && longueur % rectangle == 0 )
        reponse = largeur/rectangle * (longueur/rectangle);
    else if ( largeur % rectangle == 0 )
        reponse = largeur/rectangle * (longueur/rectangle + 1);
    else if ( longueur % rectangle == 0 )
        reponse = longueur/rectangle * (largeur/rectangle + 1);
    else
        reponse = longueur/rectangle * (largeur/rectangle) + longueur/rectangle + (largeur/rectangle) + 1 ;
    
    cout << reponse;
    
    
    return 0;
}*/
